{
  "name": "firebase-angular-template",
  "version": "1.0.0",
  "description": "Template for project with Firebase Hosting, Functions and Angular.",
  "author": "Cave Lab",
  "scripts": {
    "emulator": "concurrently \"cd functions && npm run watch\" \"firebase emulators:start --only \"database,functions\" --import=./emulator_data --export-on-exit\"",
    "emulator-no-watch": "firebase emulators:start --only \"database,functions\" --import=./emulator_data --export-on-exit"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/webfontloader": "^1.6.38",
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "webfontloader": "^1.6.28"
  }
}
